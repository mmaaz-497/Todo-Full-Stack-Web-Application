🎉 Todo AI Chatbot has been deployed!

Release: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}
Environment: {{ .Values.environment }}

═══════════════════════════════════════════════════════════

📋 DEPLOYMENT SUMMARY

Services Deployed:
{{- if .Values.apiService.enabled }}
  ✓ API Service ({{ .Values.apiService.replicaCount }} replicas)
{{- end }}
{{- if .Values.recurringTaskService.enabled }}
  ✓ Recurring Task Service ({{ .Values.recurringTaskService.replicaCount }} replicas)
{{- end }}
{{- if .Values.notificationService.enabled }}
  ✓ Notification Service ({{ .Values.notificationService.replicaCount }} replicas)
{{- end }}
{{- if .Values.auditService.enabled }}
  ✓ Audit Service ({{ .Values.auditService.replicaCount }} replicas)
{{- end }}
{{- if .Values.websocketSyncService.enabled }}
  ✓ WebSocket Sync Service ({{ .Values.websocketSyncService.replicaCount }} replicas)
{{- end }}

Infrastructure:
{{- if .Values.postgresql.enabled }}
  ✓ PostgreSQL Database (in-cluster)
{{- else if .Values.externalDatabase.enabled }}
  ✓ PostgreSQL Database (external managed)
{{- end }}
{{- if .Values.kafka.enabled }}
  ✓ Kafka ({{ .Values.kafka.replicaCount }} brokers)
{{- else if .Values.externalKafka.enabled }}
  ✓ Kafka (external managed)
{{- end }}

═══════════════════════════════════════════════════════════

🌐 ACCESS YOUR APPLICATION

{{- if .Values.ingress.enabled }}
{{- range .Values.ingress.hosts }}
Application URL: https://{{ .host }}
API Endpoint:    https://{{ .host }}/api
WebSocket:       wss://{{ .host }}/ws
{{- end }}

⏳ Waiting for SSL certificate...
Check status: kubectl get certificate -n {{ .Release.Namespace }}

{{- else }}
Get the LoadBalancer IP:
  kubectl get svc {{ .Values.apiService.name }} -n {{ .Release.Namespace }}

Access via: http://<EXTERNAL-IP>:8000
{{- end }}

═══════════════════════════════════════════════════════════

📊 MONITORING

{{- if .Values.monitoring.enabled }}
{{- if .Values.monitoring.grafana.enabled }}
Access Grafana:
  kubectl port-forward -n monitoring svc/prometheus-grafana 3000:80
  URL: http://localhost:3000
  User: admin
  Password: {{ .Values.monitoring.grafana.adminPassword }}
{{- end }}

{{- if .Values.monitoring.prometheus.enabled }}
Access Prometheus:
  kubectl port-forward -n monitoring svc/prometheus-kube-prometheus-prometheus 9090:9090
  URL: http://localhost:9090
{{- end }}
{{- end }}

═══════════════════════════════════════════════════════════

🔍 USEFUL COMMANDS

Check pod status:
  kubectl get pods -n {{ .Release.Namespace }}

View API service logs:
  kubectl logs -f deployment/{{ .Values.apiService.name }} -n {{ .Release.Namespace }}

Scale API service:
  kubectl scale deployment/{{ .Values.apiService.name }} --replicas=5 -n {{ .Release.Namespace }}

Update application:
  helm upgrade {{ .Release.Name }} ./charts/todo-app --namespace {{ .Release.Namespace }}

Rollback deployment:
  helm rollback {{ .Release.Name }} -n {{ .Release.Namespace }}

Delete deployment:
  helm uninstall {{ .Release.Name }} -n {{ .Release.Namespace }}

═══════════════════════════════════════════════════════════

⚠️  NEXT STEPS

{{- if .Values.ingress.enabled }}
1. Configure DNS to point {{ .Values.domain }} to LoadBalancer IP:
   kubectl get svc ingress-nginx-controller -n ingress-nginx -o jsonpath='{.status.loadBalancer.ingress[0].ip}'

2. Wait for SSL certificate to be issued (2-5 minutes):
   kubectl get certificate -n {{ .Release.Namespace }} --watch

3. Test API health:
   curl https://{{ .Values.domain }}/api/health
{{- else }}
1. Configure external access (LoadBalancer or Ingress)
2. Set up SSL certificates
3. Configure DNS
{{- end }}

4. Set up monitoring and alerting
5. Configure database backups
6. Review security policies
7. Set up CI/CD pipeline

═══════════════════════════════════════════════════════════

📚 DOCUMENTATION

Full Documentation: DIGITALOCEAN_DEPLOYMENT.md
Quick Start Guide:  DEPLOYMENT_QUICKSTART.md
Troubleshooting:    See docs/troubleshooting.md

═══════════════════════════════════════════════════════════

Need help? Visit: https://github.com/yourusername/todo-app/issues
